let a="33"

let IntiNum=Number(a)

let ab="33abn"  // it also convert it into numbers 
let test2=Number(ab)
console.log(test2)//we will get NaN as an output it means that shows gitchs 

let pa=null
let h=Number(null)
console.log(h)
//Here we show that null is convered into 0.